_target_: fastwam.runtime.create_causalwan22_model
model_id: Wan-AI/Wan2.2-TI2V-5B
tokenizer_model_id: Wan-AI/Wan2.1-T2V-1.3B
tokenizer_max_len: 128
redirect_common_files: false
skip_dit_load_from_pretrain: false
load_text_encoder: false

train_shift: 5.0
infer_shift: 5.0
num_train_timesteps: 1000

# Teacher-forcing conditioning noise
cond_noise_prob: 0.5   # probability of noising conditioning latents
cond_t_min: 0.5         # min timestep fraction for conditioning noise
cond_t_max: 1.0         # max timestep fraction for conditioning noise

dit_config:
  has_image_input: false
  patch_size: [1, 2, 2]
  in_dim: 48
  hidden_dim: 3072
  ffn_dim: 14336
  freq_dim: 256
  text_dim: 4096
  out_dim: 48
  num_heads: 24
  attn_head_dim: 128
  num_layers: 30
  eps: 1.0e-06
  seperated_timestep: true
  require_clip_embedding: false
  require_vae_embedding: false
  fuse_vae_embedding_in_latents: true
  use_gradient_checkpointing: true
  video_attention_mask_mode: "per_frame_causal"
  action_conditioned: false
